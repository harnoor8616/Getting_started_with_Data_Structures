#include<iostream>
#include<vector>
using namespace std;
int main()
{
    int nodes=5,edges=6,u,v;
    vector<vector<int>> adjlist(nodes);
    for(int i=0;i<nodes;i++)
    {
        cout<<"Enter edges (u,v):"
    }
}